import { FormatTruckMatriculaPipe } from './format-truck-matricula.pipe';

describe('FormatTruckMatriculaPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatTruckMatriculaPipe();
    expect(pipe).toBeTruthy();
  });
});
